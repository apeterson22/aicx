[build-system]
requires = ["maturin>=1.15.0,<2"]
build-backend = "maturin"

[project]
name = "aicx"
version = "0.1.0"
description = "Python bindings for the Rust-first AICX archive engine"
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "aicx"
